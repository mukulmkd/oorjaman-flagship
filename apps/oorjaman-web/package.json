{
  "name": "oorjaman-web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run brand:sync && next dev --port 3000",
    "build": "npm run brand:sync && next build",
    "build:uat": "npm run brand:sync && OORJAMAN_STATIC_EXPORT=1 NEXT_PUBLIC_DEPLOY_ENV=uat next build",
    "build:godaddy": "npm run brand:sync && OORJAMAN_STATIC_EXPORT=1 next build",
    "build:vercel": "npm run brand:sync && next build",
    "start": "next start --port 3000",
    "brand:sync": "node ../../scripts/sync-brand-assets.mjs --web-only",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@oorjaman/config": "*",
    "next": "^15.5.2",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "eslint": "^9.28.0",
    "eslint-config-next": "^15.5.2",
    "typescript": "~5.7.2"
  }
}
