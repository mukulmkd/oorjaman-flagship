const PREFIX = "oorjaman_technician_";

/** Bumped when pre-login onboarding copy/layout changes materially. */
export const STORAGE_KEY_ONBOARDING = `${PREFIX}onboarding_complete_v3`;
/** Set after user sees the location primer (grant or skip). */
export const STORAGE_KEY_LOCATION_PROMPT_DONE = `${PREFIX}location_prompt_done`;
